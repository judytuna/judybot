# Core requirements
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.21.0
peft>=0.7.0
trl>=0.7.0
bitsandbytes>=0.41.0

# Enhanced training with Unsloth (optional but recommended)
# Install with: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Flash attention (optional, for even better performance)
# Install with: pip install flash-attn --no-build-isolation

# Utilities
pyyaml>=6.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.65.0

# Development
jupyter
ipywidgets